# <Feature name>

> Спека — это **SDD** (AEF Standard 2), а не план работ: она описывает *что и почему*,
> а не *как*. И это **замкнутая модель** (Standard 5): всё, что фича вводит, перечислено
> поимённо в `Entity inventory`. Формулировки открытого множества («и т.п.», «и др.»,
> «и подобные») запрещены — они делают дрейф кода от спеки необнаружимым.
>
> Валидность проверяется до передачи агенту; невалидная спека в работу не уходит.
>
> Класс документа: **K1 — Executable** (Standard 6) — читает агент во время работы.

## Owner
<!-- Standard 2, обязательное поле. Конкретный человек, занимающий роль `Engineer`
     (Том II §1.2) на этой задаче: отвечает за использование агента и исход сессии,
     принимает решения на status=paused / escalated.
     — Роль вместо имени не допускается: роль уже задана определением.
     — Это не авторство спеки: автор вписывается только если он же занимает роль Engineer.
     — Заполняется не позже gate Specification → Architecture; без него спека агенту не уходит.
     Прочие слои ответственности (архитектура, безопасность, риск) сюда не дублируются —
     они достигаются консультацией по таблице Том II §1.2 (см. CALIBRATION.md). -->

- `<Имя Фамилия>`

## JMS reference
- JMS 2.0 § `<section>`, interfaces/methods covered.

## Current state in tiny-mq
- What exists today (file:line citations).
- What is missing.

## Proposed API
- Public C++ signatures to add or change (header-level).
- Для каждого затронутого модуля — контракт в языконезависимой форме (Standard 10):
  предусловия, постусловия, инварианты состояния. Модуль без объявленного контракта
  не имеет проверяемой границы и не может быть передан агенту как единица работы.

## Semantics
- Behaviour, ordering, concurrency, error conditions.
- Interaction with acknowledge modes, transactions, durable subscribers.

## Entity inventory
<!-- Standard 5: замкнутый машиночитаемый реестр. Список с устойчивыми ID, не проза.
     Сущность, появившаяся в коде и отсутствующая здесь, — дрейф и блокер до MR.
     Реестр правит автор спеки/оркестратор, не producer и не reviewer. -->

| ID | Kind | Name | Note |
|----|------|------|------|
| `E1` | type | `<Class>` | |
| `E2` | method | `<Class>::<method>` | |
| `E3` | header | `<JMSXFoo>` | имя свойства/заголовка |
| `E4` | storage-field | `<field> @ offset <N>` | поле формата `0x02` |
| `E5` | test-suite | `<Suite>` | |

Kind ∈ `type` · `method` · `header` · `storage-field` · `config` · `test-suite`.
Пустой реестр = спека невалидна.

## Persistence / wire implications
- New fields serialised into `Message::toBytes()` or storage?
- New protocol frames or control messages?

## Dependencies
- Other specs that must land first.

## Stop-conditions
<!-- Standard 2, обязательное поле. Условия остановки ВНУТРИ уже начатой работы —
     не то же, что «делать ли задачу вообще» (это решается до старта).
     Producer, столкнувшись с любым из них, пишет status=paused и вопрос человеку.
     Молчаливого умолчания при неоднозначности не существует: неопределённая граница —
     не свобода агента, а его обязанность остановиться. -->

- `<неоднозначность 1: что именно неясно и почему выбор нельзя сделать молча>`
- `<неоднозначность 2>`

Общие, действующие для любой спеки:
- требуется отклонение от унаследованных ограничений (формат `0x02`, threading-модель
  ADR-0005, объектная модель, durable-ключ) — это ADR, а не решение producer'а;
- нужна сущность, отсутствующая в `Entity inventory`;
- нужен файл вне объявленного `target_files`;
- пункт `Test plan` невозможно выразить как исполняемый GTest-критерий.

## Test plan
- Unit tests to add under `tests/` (follow existing `*Test.{h,cpp}` pattern).
- Каждое утверждение `Semantics` имеет пункт здесь, и наоборот (проверяет `spec-critic`
  до реализации — автор критериев не может быть единственным судьёй их полноты, Standard 3).
- Для изменяемых заголовков тест обязан покрывать **персистентный** случай (ADR-0008).

## Rollback
- Как откатить фичу, если она окажется неверной: удаляемые файлы, обратимость формата
  хранения, влияние на уже записанные данные.

## Autonomy Level
- `R2` по умолчанию (изолированный worktree, приёмка человеком).
- `R1` для необратимого: изменение формата хранения, публичного API, сборочной среды.

## Open questions
- Вопросы, не влияющие на реализацию. Всё, что на неё влияет, обязано переехать
  в `Stop-conditions` или быть решённым до передачи агенту.

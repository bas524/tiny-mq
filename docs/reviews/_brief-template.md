# Объяснение изменения — <NN> <название>

> **Это вход человеческого решения** (AEF Standard 4), а не его подтверждение. Здесь нет вердикта
> о корректности: её подтверждают гейты, отчёты которых перечислены ниже по ссылкам.
> Каждое утверждение несёт источник. Утверждение без источника — дефект этого документа.
>
> Класс документа: **K4 — Historical** после приёмки (до приёмки — рабочий вход решения).
> Разделы 2, 3 и 7 — по применимости; неприменимый раздел удаляется с одной строкой почему.

- **Спецификация:** `docs/jms-spec/<NN>-<slug>.md`
- **Адресат решения:** `<Owner или назначенный рецензент>`
- **Дельта:** `openspec/changes/<NN>-<slug>/specs/<capability>/spec.md`
- **Отчёты гейтов:** `docs/reviews/<NN>-<slug>.review.md`, `<NN>-<slug>.nfr.md`, …
- **Составлено:** `<модель, дата>` — стадия объяснения, независима от producer

## 1. Что изменится в поведении

<!-- Из дельты capability-спеки: добавленные, изменённые и удалённые требования — словами того,
     кто пользуется продуктом, а не словами кода. Каждый пункт — со ссылкой на требование и тест. -->

| Что увидят иначе | Было | Стало | Источник |
|---|---|---|---|
| <наблюдаемое поведение> | <…> | <…> | `Requirement: <имя>`; тест `<Suite.Case>` |

**Что НЕ изменилось, хотя можно было ожидать:** <…> — ссылка на требование или `Open questions`.

## 2. Что изменилось в устройстве *(если задеты границы или ADR)*

<!-- Схема в формате mermaid, исходником. Помечай: новое, удалённое, переехавшее.
     Схема без пометок, что на ней изменилось, читателю не помогает. -->

```mermaid
<схема: было → стало>
```

- **Появилось:** `<сущность>` — `файл:строка`
- **Изменило границу или зависимость:** `<что и куда>` — `файл:строка`, ADR `<NNNN>`
- **Удалено:** `<что>` — `файл:строка`

## 3. Изменённый сценарий *(если менялся порядок, протокол или жизненный цикл)*

```mermaid
<последовательность или состояния: только изменившийся участок>
```

Что в сценарии новое и почему — со ссылкой на требование.

## 4. Связь с намерением

| Требование спецификации | Чем закрыто | Чем подтверждено |
|---|---|---|
| `<Requirement: имя>` | `файл:строка` | `<Suite.Case>` |

Если требование закрыто не так, как предполагала спецификация, — **сказать здесь прямо**, со ссылкой
на место расхождения. Это то, ради чего человек читает объяснение.

## 5. Точки решения

<!-- 3–5 мест, где выбор был неочевиден. Источники: остановки paused, вопросы критика, вердикты
     гейтов, ADR. Не «всё в порядке», а «здесь была развилка и выбрано вот это».
     Если развилок не было — так и написать, с ссылкой на журнал. -->

| Развилка | Что выбрано | Кто решил | Источник |
|---|---|---|---|
| <в чём была неоднозначность> | <выбор> | `Owner` / гейт / ADR | `chain.log`, пакет, `docs/reviews/…` |

## 6. Цена отката

- **Откатывается:** <что и как> — `Rollback` спецификации.
- **Уже необратимо:** <изменения формата данных, публичных контрактов, внешние эффекты> — или «нет».
- **Класс обратимости** (Standard 21): `Reversible | Controlled | Irreversible`.

## 7. Демонстрация *(по применимости)*

<!-- Единственная часть этого документа, являющаяся свидетельством: человек запускает её сам. -->

```
./cmake-build-debug/tiny_mq --gtest_filter=<Suite>.<Case>
<шаги сценария: что сделать и что увидеть — до и после>
```

Наблюдаемость не поднята — раздел удаляется с пометкой, а не заполняется рассуждением.
